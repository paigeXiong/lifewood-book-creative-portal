# 发布准备检查记录（2026-09-07）

本次在 `C:\Aigc` 检查当前未提交改动。此记录表示本地验证结果，不表示已经提交 GitHub 或发布 Release。

## 已通过

| 检查 | 结果 |
| --- | --- |
| `npm run verify` | 客户端 194、管理端 57、服务端 154 个测试通过；运维及备份特殊文件名回归通过；两套前端生产构建通过 |
| 客户到交付的完整接口回归 | 新增 `CustomerJourneyFromDraftThroughRevisionToFinalDownload`，单独执行通过（1 个）；在上述 154 个服务端测试之后新增 |
| `npm run test:e2e` | 1 个浏览器测试通过，覆盖首次创建管理员、进入用户管理和切换英文 |
| `npm run publish:aot` | Windows win-x64 Native AOT 发布通过 |
| `npm run smoke:aot` | 独立数据目录下的 Native AOT 启动冒烟通过 |
| 移动端只读检查 | 390×844 视口检查客户项目列表、退回后进入指定单元、打开反馈面板；退回编辑页未见横向溢出，反馈内容及回复入口可见 |

完整接口回归通过真实 HTTP 接口完成：创建客户草稿、填写资料、上传封面、首次提交、管理员退回、客户修改并回复、重新提交、管理员上传成品、客户端下载并校验字节。测试使用独立数据，不改动开发环境的业务项目。

检查移动页面时发现本地 5077 后端没有运行，已使用原数据目录恢复；恢复后客户页面正常加载。本次未确定该进程停止的原因。

## 文档同步

- 产品需求中的待确认事项已移除已实现的后台配置、语言路由及定向退回等内容。
- UI 规范已明确批准的书籍资料辅助识别范围，避免与旧的 AI 排除描述冲突。
- 生产协作、客户验收、组织共享等仍为待确认需求，不在本次扩展实现。

## 发布前仍需完成

- 推送后运行 Linux CI，确认 Linux 构建和运行结果；本机 Windows 验证不能替代它。
- 确认版本号、生成正式发布包并发布 Release；本次没有执行提交、推送或发布。
- Android 夸克浏览器调用系统相机后的恢复行为没有在本次真机重测；移动视口检查不能替代真机拍照验证。

管理端生产构建仍有现有的大包体积提示，构建成功。本次未扩大为包体优化。

本地详细日志位于忽略目录 `.tmp/`：`release-verify.log`、`release-e2e.log`、`release-aot.log`、`journey-regression.log`。


## 公告功能加入后的补充验收

- `npm run verify` 通过：客户前端 196、管理前端 57、服务端 160 项，运维及备份回归通过，两套前端生产构建通过。
- 浏览器端到端用例已扩展到公告发布和客户查看：独立数据环境下发布双语公告、验证公开语言筛选、390px 下加载 23 条公告、关闭后刷新以及打开个人历史，最终通过（1 个完整用例）。
- 验收发现逐条关闭多条公告会触发写入限流；已改为按账号批量关闭。修复后的公告服务端 6 项、前端 2 项测试通过，独立代码审查无遗留阻塞项。完整 160 项回归先于此修复，未宣称重跑新增后的全部 161 项。
- 公告验收日志：`.tmp/announcement-full-verify.log`、`.tmp/announcement-e2e.log`、`.tmp/announcement-bulk-tests.log`、`.tmp/announcement-ui-tests.log`。
- Linux x64/ARM64 CI 与安卓夸克真机仍未执行；未提交、推送或发布新版本。

- 批量关闭修复后的 Windows Native AOT 发布与启动冒烟均通过；最终客户前端构建通过，本地 5077 健康检查返回 200。日志：`.tmp/announcement-final-aot.log`。
