# 2026-09-10 备份恢复与运行配置验收

下午计划的恢复设计、实现、后台入口、异常测试、代码审查及发布资料整理已完成。版本已定为 v0.3.12；通过本地最终验收后提交，跨平台构建全部成功后公开 Release。

## 当前交付

- 手动与定时备份、保留策略、下载和服务端筛选分页。
- 恢复预检、确认、安全备份、独立进程切换、启动确认和失败回退。
- 独立恢复历史、分类失败说明、关联操作人及安全备份下载。
- 后台独立校验、结果筛选、检查时间与中断重试；结果存储失败不会显示成功。
- 三组监听配置、运行健康展示及审计详情、CSV 导出。
- 配额扫描处理枚举后文件消失，权限和其他读取错误仍阻止继续上传。

## 最终验证

| 检查 | 结果 | 本地证据 |
|---|---|---|
| 客户门户单元测试 | 233 项通过 | `.tmp/v0312-verify.log` |
| 管理后台单元测试 | 87 项通过 | 同上 |
| 后端全量测试 | 288 项通过 | 同上 |
| 运维备份、特殊文件名与恢复脚本回归 | 通过 | 同上 |
| 两套前端生产构建 | 通过 | 同上 |
| 完整浏览器验收 | 7 项通过，含中英文及移动端 | `.tmp/v0312-e2e.log` |
| Windows x64 Native AOT | 构建及运行冒烟通过 | `.tmp/afternoon-aot.log`、`.tmp/afternoon-aot-smoke.log` |
| 原生程序恢复与启动失败回退 | 通过 | `.tmp/afternoon-restore.log` |
| CI YAML、差异格式及增量代码审查 | 通过 | 工作流解析、`git diff --check`、只读审查 |

最终隔离恢复演练目录：`artifacts/managed-restore/42f73e1b-7011-4383-bf9b-1342350537f0`。核对了数据回到备份、安全归档保留恢复前文件、旧会话失效、当前备份策略保留、新服务启动与失败自动回退。所有演练仅使用随机测试目录，未恢复本地业务数据。

Windows 验证和发布工作流现已加入对本次构建原生程序的恢复演练；失败会阻止对应发布继续。GitHub Actions 尚未在本轮触发。

## 已修复与保留观察

- 已修复恢复提交、启动重试、Windows 日志替换、一次性重启参数及恢复历史刷新等审查问题。
- 已修复校验结果写盘失败仍发布成功终态，新增持久化失败及重新排队测试。
- 首轮全量验收出现一次参考图片上传 500；单例复测和后续完整后端复测均通过。检查中独立确认并修复配额扫描的文件消失风险，但缺少该次异常堆栈，不能确认它就是原失败根因。若再现，应按请求标识提取服务端异常，不以自动重试掩盖问题。
- 前端已有主包超过 500 kB 的构建提示，构建正常完成。

## 发布边界

- 后台恢复适用于独立进程；Windows 服务和 systemd 继续使用停机恢复流程。
- Linux 实机与对应架构 CI 本轮未执行，不能据 Windows 结果声称 Linux 已验证。
- 邮件、备份加密、异地复制未纳入本次交付。
- 已统一版本号 v0.3.12；公开发布由标签构建完成后自动执行。

正式发布说明见 [v0.3.12](./releases/v0.3.12.md)。操作与人工回退步骤见 [后台备份管理](./managed-backups.md)。

## 当日追加验收

- 组织横向文字标识、设置导航图标和滑动选中效果。
- 客户任务列表固定列宽与工具栏尺寸，避免切换待处理时页面错位。
- 个人资料按需编辑，统一两端账号菜单、头像姓名整体样式及个人资料跳转入口。
- 前端不可信文本与链接安全审查；补齐渲染协议白名单，并通过恶意 HTML 及危险 URL 验证。独立复核未发现可证实的新 XSS 路径。
- 本次发布前增量只读代码复核未发现 P1/P2 阻断问题。
