# v0.3.10 验收记录（2026-09-08）

## 已完成

- 需求基线、客户详细需求和开发基线同步已批准的定向退回、公告及站内通知，移除相互冲突的旧描述。
- 最终全量验证通过：客户端 202 项、管理端 59 项、后端 201 项；运维及备份特殊文件名回归、两套前端生产构建通过。
- 浏览器完整用例通过（约 2.3 分钟）：双语公告、公示天数、历史兼容、批量关闭、通知已读/归档/偏好/跳转、管理规则、SSE 阻断后的列表轮询同步。
- 中英文 390px 角色页间距至少 15px，无横向溢出；客户顶栏公告、通知使用带本地化无障碍名称的紧凑图标。
- Windows Native AOT 发布和独立目录启动冒烟通过。
- 从本地 v0.3.9 标签导出服务端测试快照，生成真实项目、封面、最终成品及旧双语公告。升级至当前 DLL 及 Native AOT 两种形态均通过；连续启动两次，已有表内容、业务文件、密钥、关闭记录保持不变，不补发历史事件；新提交生成且仅生成一条通知。
- 升级比较前先以旧版重启归一化其既有的草稿工作流时间字段；仅排除数据库运行文件和 platform.lock 的文件哈希，已有业务表仍逐条比较。使用随机 artifacts/upgrade-check 子目录，不接触开发业务数据。
- 独立代码复核发现的三项问题已修复并复核通过：历史回复失效跳转、断线列表刷新、成功批次错误占用连续失败次数。

## 可复现升级验证

从 v0.3.9 标签导出并编译 services/platform-api，随后运行：

```powershell
python scripts/test-notification-upgrade.py --old-server <旧版服务端DLL路径> --new-server artifacts/aot-local/Lifewood.BookPortal.Server.exe
```

测试默认使用 5096 端口，运行前检查占用；证据留在随机隔离目录。Python 仅使用标准库，不需要额外依赖。

## 本地证据

日志位于忽略目录 .tmp：2026-09-08-final-verify.log、2026-09-08-e2e.log、2026-09-08-aot.log、2026-09-08-aot-smoke.log、2026-09-08-upgrade-aot.log。截图位于 artifacts/character-spacing-mobile-zh-CN.png、character-spacing-mobile-en-US.png、notification-center-mobile-en.png。

## 发布与限制

- 用户已明确授权提交 GitHub 并发布 v0.3.10。发布通过 origin/main 的 Verify 和版本标签触发的 Release packages 工作流执行；以远端 Actions 状态作为最终发布结果。
- 本地 Windows ZIP 和 zh-CN/en-US MSI 已生成，MSI 数据路径与数据保留检查通过。构建发现并修复 SDK 默认包含本地 data 目录的问题，项目显式排除 data/**，现有包安全检查保留。
- 安卓夸克系统相机返回没有真机复测；窄屏浏览器检查不能代替此项。
- 管理端仍有既有的大包体积提示，构建成功。
- 生产协作、在线审片、多版本审批和外部通知渠道继续保留为待确认范围。


## 用户视觉反馈后的样式补充

- 补齐共享通知入口、弹窗按钮、搜索/下拉控件、偏好表单及客户通知中心容器样式，避免依赖管理端的全局控件样式。
- 使用独立 E2E 数据检查客户/管理两端、中文/英文及 1280px/390px 视口，覆盖快捷弹窗和偏好表单；两端构建通过。
- 正式发布不复用此前的本地安装包；GitHub Actions 从 v0.3.10 标签重新构建，包含后续全部改动。

- 按后续界面反馈，将通知操作及顶栏通知/公告入口统一为无背景、无可见文字的 SVG 图标按钮；保留双语 title、aria-label、焦点与禁用状态，双端双语桌面/手机浏览器检查通过。


## 最终发布范围补充

- 账号快捷切换、设备会话保存和跨页请求隔离；运营角色、细分操作权限及负责人范围校验。
- 客户删除已退回项目及关联记录/文件清理；项目列表回车搜索、单行工具栏和待处理统计。
- 通知的已读/未读视觉差异、固定高度弹窗与加载失败保护。
- 运营双账号真实接口验证覆盖列表、详情、附件、快照、声音、退回、回复、交付、改派、降权与停用；中英文 1280px/390px 浏览器检查通过。
- 新增发布前复核会覆盖最终工作树及 Native AOT 序列化注册。发布日志保留在忽略目录 `.tmp/v0.3.10-release-*.log`，远端安装包直接由版本标签对应提交生成。

- 最终本地 `npm run verify` 通过：客户前端 202、管理端 59、后端 201 项测试；运维回归和两端生产构建通过。独立发布审查未发现阻断问题，版本、序列化注册、升级兼容与敏感文件排除检查通过。
- 最终 `npm run test:e2e` 通过，完整双语业务浏览器用例约 2.3 分钟；日志 `.tmp/v0.3.10-release-e2e.log`。


## 远端发布记录

- v0.3.10 对应产品提交 `155f233be964439646a1db460d8494418e60ce50`，Release packages 工作流 `34192209494` 全部通过，五种安装包和五份 SHA-256 文件已公开，校验文件与 GitHub 资产摘要一致。
- 首轮 Verify 的浏览器用例在历史公告数据尚未返回时提前滚动，导致分页断言失败。后续仅修正测试等待顺序，先等待公告条目加载再滚动；本地完整 E2E 复验通过，产品源代码和已发布安装包未变更。
- 最新 Verify 状态以 [GitHub Actions](https://github.com/paigeXiong/lifewood-book-creative-portal/actions/workflows/verify.yml) 为准。
